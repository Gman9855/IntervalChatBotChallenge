# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'IntervalChallenge' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for IntervalChallenge
  pod 'Socket.IO-Client-Swift'
  pod 'MessageKit'
  
  target 'IntervalChallengeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'IntervalChallengeUITests' do
    # Pods for testing
  end

end
